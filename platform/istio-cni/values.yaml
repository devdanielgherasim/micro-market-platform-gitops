profile: ambient

global:
  istioNamespace: istio-system

cni:
  ambient:
    enabled: true
  excludeNamespaces:
    - istio-system
    - kube-system

resources:
  requests:
    cpu: 100m
    memory: 128Mi
  limits:
    cpu: 300m
    memory: 256Mi

global:
  cloudProvider: azure
  environment: dev
  storageClass:
    aws: gp3
    azure: managed-csi
    gcp: standard-rwo

cluster:
  name: postgresql
  instances: 2
  imageName: ghcr.io/cloudnative-pg/postgresql:16.4
  database: microservices
  owner: microservices_owner
  ownerSecret: postgresql-auth
  size: 10Gi
  # Applied when global.environment == "dev" (single instance, right-sized
  # for a dissertation-scale cluster). See templates/cluster.yaml.
  devResources:
    requests:
      cpu: 100m
      memory: 256Mi
    limits:
      cpu: 500m
      memory: 512Mi
  # Applied for every other environment.
  resources:
    requests:
      cpu: 200m
      memory: 512Mi
    limits:
      cpu: 1
      memory: 1Gi
  databases:
    - name: catalog
      owner: catalog
    - name: orders
      owner: orders
    - name: audit
      owner: audit
    - name: keycloak
      owner: keycloak
  roles:
    - name: catalog
      passwordSecret: catalog-db-secret
    - name: orders
      passwordSecret: orders-db-secret
    - name: audit
      passwordSecret: audit-db-secret
    - name: keycloak
      passwordSecret: keycloak-postgresql
